version: '3.8'

services:
  backend:
    build: .
    container_name: restaurento-backend
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production